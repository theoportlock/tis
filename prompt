A system that reads, recognises, and writes bitarrays
data interpreted as an integer but saved as a binary (like DNA)
predicts new input bits
commits combinations of bits starting from a base layer and entering combination space
event driven - pyventus
Looks for combinations between bits, pinches a memorry array in combinations space to find all of the previously observed inupts, work out the probability of each bit within the input bitarray ( a bit similar to the fishers test with the odds and significce that the bit is a 1) and then saves the predicted input bitarray.
